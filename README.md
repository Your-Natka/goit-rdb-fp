# goit-rdb-fp

# Final Project — Relational Databases

## 📌 Опис проєкту
Фінальний проєкт з курсу реляційних баз даних.  
Мета — імпортувати дані, нормалізувати таблицю до 3-ї нормальної форми, виконати аналітичні SQL-запити та створити власну SQL-функцію.

База даних побудована на основі статистики інфекційних захворювань.

---

## 🛠 Використані інструменти
- MySQL 8.x
- MySQL Workbench
- SQL

---

## 📂 Структура репозиторію

goit-rdb-fp/
│
├── sql/
│ └── fp.sql
│
├── screenshot/
│ └── p1_1.png
│ └── p1_2.png...
│
└── README.md

✅ Завдання 1. Створення схеми та імпорт даних
- Створено схему `pandemic`
- Схему встановлено як активну
- Дані імпортовано у таблицю `infectious_cases`
- Перевірено кількість завантажених записів

✅ Завдання 2. Нормалізація даних

Таблицю infectious_cases нормалізовано до 3-ї нормальної форми.

Створено:

таблицю entities (унікальні Entity та Code)

таблицю infectious_cases_norm з зовнішнім ключем entity_id

Дані перенесено з урахуванням усунення повторюваних атрибутів.

✅ Завдання 3. Аналітичний запит

Для кожної унікальної сутності виконано розрахунок:

середнього значення

мінімального

максимального

суми захворювань Number_rabies

Порожні значення попередньо відфільтровано.
Результат відсортовано за середнім значенням у спадному порядку та обмежено 10 рядками.

✅ Завдання 4. Різниця в роках (вбудовані функції)

На основі колонки year побудовано:

дату YYYY-01-01

поточну дату

різницю в роках між ними

Для розрахунку використано стандартні SQL-функції.

✅ Завдання 5. Власна SQL-функція

Створено користувацьку функцію year_diff_from_now, яка:

приймає рік

створює дату YYYY-01-01

повертає різницю в роках між цією датою та поточною датою.

```sql 
CREATE FUNCTION year_diff_from_now(input_year INT)
RETURNS INT
DETERMINISTIC
BEGIN
    RETURN TIMESTAMPDIFF(
        YEAR,
        STR_TO_DATE(CONCAT(input_year, '-01-01'), '%Y-%m-%d'),
        CURDATE()
    );
END;

Функцію успішно застосовано до нормалізованої таблиці.